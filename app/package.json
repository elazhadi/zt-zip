{
  "name": "ulysse70-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*", "frontend"],
  "scripts": {
    "test": "npm run test --workspace=packages/moteur",
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend"
  }
}
